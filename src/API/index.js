const ENDPOINT = "http://127.0.0.1:8000/";

export const API = {
  ADD_BUDGET: `${ENDPOINT}add-budget/`,
  ADD_PAYDAY: `${ENDPOINT}add-payday/`,
  GET_BUDGET: `${ENDPOINT}get-budget/`,
  GET_PAYDAY: `${ENDPOINT}get-payday/`,
  GET_ALL_COSTS: `${ENDPOINT}get-costs-all/`
};
